app:
  debug: false
  title: SIU Salukis NAV Scoring
  version: 1.0.0
backup:
  backup_path: /app/data/backups
  enabled: true
  frequency_hours: 24
  max_backups: 10
  retention_days: 7
database:
  path: /app/data/navs.db
email:
  recipients_coach: mike@YOUR_DOMAIN.com
  sender_email: mike@nssttw.com
  sender_name: SIU Flying Salukis NAV Scoring
  sender_password: Y0TdPWQ8LmCt
  smtp_host: smtp.zoho.com
  smtp_port: 587
  smtp_username: mike@nssttw.com
prenav:
  token_expiry_hours: 48
scoring:
  checkpoint_radius_nm: 0.25  # Configurable per NIFA
  fuel_burn:
    over_estimate_multiplier: 250  # Changed from 500 (Red Book v0.4.6)
    under_estimate_multiplier: 500  # Changed from 250 (Red Book v0.4.6)
    over_estimate_threshold: 0.1  # 10% threshold for overestimate (new)
  off_course:
    checkpoint_radius_nm: 0.25  # Radius in NM
    min_penalty: 100  # Penalty at (radius + 0.01) NM
    max_penalty: 600  # Penalty at 5.0 NM
    max_distance_nm: 5.0  # Max distance for penalty calculation
  secrets:
    checkpoint_penalty: 20
    enroute_penalty: 10
    max_distance_miles: 1.0
  timing_penalty_per_second: 1.0
session:
  cookie_httponly: true
  cookie_name: nav_scoring_session
  cookie_secure: true
  timeout_hours: 24
storage:
  gpx_uploads: /app/data/gpx_uploads
  pdf_reports: /app/data/pdf_reports
